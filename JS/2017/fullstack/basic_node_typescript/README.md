How to 'typescriptify node app"

# make sure npm install @types/node --save-dev so that vscode understand typescript for node
# then add tsconfig.json to guide typesript on how to compile
    this also includes 